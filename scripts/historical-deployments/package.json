{
  "name": "deployment-frequency",
  "version": "1.0.0",
  "description": "",
  "main": "app.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "npm run lint:js && npm run lint:format",
    "lint:js": "eslint --report-unused-disable-directives --config ./.eslintrc.json '**/*.{js,jsx,ts,tsx,mjs}'",
    "lint:js:fix": "npm run lint:js -- --fix",
    "lint:format": "prettier --check '**/*'",
    "lint:format:fix": "prettier --write '**/*'"
  },
  "author": "dxw",
  "license": "ISC",
  "dependencies": {
    "@influxdata/influxdb-client": "^1.18.0",
    "@octokit/rest": "^18.11.0",
    "dotenv": "^10.0.0"
  },
  "devDependencies": {
    "@wearedxw/eslint-config": "^2.0.0",
    "eslint": "^7.32.0",
    "prettier": "^2.4.1"
  }
}
